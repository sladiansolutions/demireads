/**
 * App shell. Phase 0 is scaffold only: no screens and no routing yet.
 * Phase 1 replaces this with the shell, Home, Letter Garden and Family Book.
 */
export default function App() {
  return (
    <main
      style={{
        height: '100%',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        fontFamily: 'var(--font-display)',
        fontSize: 'var(--size-heading)',
      }}
    >
      Ready to build.
    </main>
  );
}
